[pytest]
# This file outranks a [tool.pytest.ini_options] section in a future
# pyproject.toml, so adding packaging metadata cannot silently lose these
# settings -- but moving them there and deleting this file would.
testpaths = tests
markers =
    slow: numerical regression gate, run at phase boundaries (deselect with '-m "not slow"')
